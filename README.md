# The `RiemannComplexNumbers` package

This is a Julia module that redefines some `Complex` operations to give (to this author's taste) mathematically more sensible results. 

**This is a work in progress**
